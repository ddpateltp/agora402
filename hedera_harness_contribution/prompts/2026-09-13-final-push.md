# Final push brief, 13 September 2026 (given to Claude Code from the fork root)

You are helping me finish my ETHOnline 2026 submission for the Hedera "Open Source: Improve the Hedera Harness" track (2 winners, USD 1,000 each) and, with the same submission, the Hedera "AI & Agentic Payments" track (3 winners, USD 2,000 each). Work in priority order, stop and ask before anything irreversible (git push, opening a PR, posting comments), and keep every change small enough to review in minutes.

Hard deadline: Submissions close Sunday 13 September 2026 at 12:00 EDT = 18:00 CEST = 21:30 IST. Late submissions are not accepted. Judging is asynchronous: the judges only see the ETHGlobal project page, the demo video and the PR pages. Nothing else counts. Work backwards from that: by 15:00 CEST all code must be pushed, by 16:30 CEST the video must be uploaded, by 17:30 CEST the ETHGlobal form must be submitted.

Repository facts (verified 12 September 2026): upstream `hedera-dev/hedera-harness`, default branch for 2.0 work is `dev` (schema v3, release 2.0.0-rc.4), `master` is the 1.2 line, all PRs must target `dev`. My fork `ddpateltp/hedera-harness` with open PRs #67 (`feat(smoke): x402 gate`) and #68 (`feat(run): show agent spend on every attempt and stop at budget.maxCostUsd`). Competition: 29 hackathon PRs from 22 authors; crowded themes are mirror-node CHAIN verification, doctor checks and x402; the cost budget PR is the only one of its kind. Open issue #8 (kantorcodes): opt-in `validators.holGuard` static scanner in ASSERT. Maintainer preferences quoted from review comments: retarget onto dev; offline stubbed tests are the bar; stage names ASSERT / SMOKE / EVALUATE / CHAIN; opt-in additive validators, run the scan after the build commands; infra errors must use the existing abort path, do not let ASSERT throw out of the attempt loop; doctor checks next to `checkChainEnv` in the shared preflight. Repo conventions: commit subjects `feat(scope): plain-English claim`; tests `node --test test/*.test.mjs`; CHANGELOG maintained; docs in `docs/authoring-a-recipe.md`; prompts in `prompts/`; `KNOWN_SPEC_KEYS` must list any new top-level key.

Judging criteria: Hedera track qualification via open PR (not merged is fine), public PR link with description explaining the problem and how to run it, demo video of 5 minutes or less; extra points for new coverage, ergonomics, tests + docs + examples, before-and-after evidence. ETHGlobal rules: video 2 to 4 minutes, 720p, real narration, AI tool usage disclosed, prompts and planning artefacts in the repo, no single-large-commit repos, select Hedera as one of up to three partner prizes.

Tasks, in priority order:
1. Harden the two existing PRs: rebase onto upstream/dev, run tests and build, report before pushing; edit the #67 description (offline tests line, Related PRs #44 and #54); edit the #68 description (Related #57 codex preset, AI tooling sentence); confirm CHANGELOG and docs cover both features with complete YAML examples.
2. Split the stream-parsing race fix out of #68 into `fix/await-agent-stream-lines` with one offline test reproducing the race; commit subject `fix: the last line an agent prints must be parsed before the process is reported closed`; PR body Problem / What this changes / How to verify / Before and after; rebase #68 on top and note "Stacked on".
3. Implement issue #8 on `feat/assert-hol-guard` following PR #40's shape and the maintainer's review: `validators.holGuard: { enabled, failOnSeverity, command? }`, run after build commands, native findings with stable ids `hol-guard:<rule>:<file>:<line>`, tooling failures through the infrastructure abort path, doctor check, fully offline tests with fixtures in `test/fixtures/hol-guard/`, docs, CHANGELOG, repair prompt line, PR body ending `Closes #8`, credit kantorcodes. Stop if not green by 14:00 CEST.
4. Before-and-after evidence: demo scripts (outside the PR branches) for the x402 gate, the cost budget and the race fix, with exact commands and expected output.
5. Four-minute video script with timings and a shot list.
6. ETHGlobal submission text: title, tagline, short description, full description covering both tracks, How it's made, AI disclosure paragraph in the prescribed form, partner feedback paragraph.
7. Outreach text: Discord message and one reviewer comment per PR.

Constraints: never push, open PRs, post comments or submit forms without an explicit yes; no secrets in code, tests, fixtures, docs, scripts or video; granular commit history; match the repo's style; British English; no em dashes; report progress after each task.
